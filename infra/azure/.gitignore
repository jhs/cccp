# Terraform — local state contains secrets; never commit it.
*.tfstate
*.tfstate.*
.terraform/
crash.log

# Variable overrides may carry sensitive values.
*.tfvars

# The provider lock file (.terraform.lock.hcl) is intentionally NOT ignored —
# it should be committed once `terraform init` generates it.
